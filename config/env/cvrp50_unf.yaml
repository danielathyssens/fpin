# @package _global_
# problem args

problem: "cvrp"
graph_size: 50
coords_dist: 'uniform'
weights_dist: 'random_int'  # sampling integers from 1-10
out_name: ${problem}_${graph_size}_${coords_dist}
data_file_path: data/test_data/cvrp/uniform/cvrp50/fc-cvrp_seed213298_size128.pt
normalize_data: True  # False --> only for train
checkpoint_load_path: models/PIM/PIM/logs/outs_vrp50/VRP_model.pth
# models/PIM/PIM/logs/outs50_new3/VRP_model.pth
# train
fixed_train_set: data/train_data/cvrp/${coords_dist}/targets/converted_npz/

# RP Env cfg
env_kwargs:
  generator_args:
    coords_sampling_dist: ${coords_dist}
    weights_sampling_dist: ${weights_dist}
    normalize_demands: True  # if None or False - demands will remain un-normalized (not between 0-1)
    single_large_instance: # data/train_data/${problem}_10000/${coords_dist}/single_large_instance.pt
    add_base_node_ids: False
  sampling_args:
    sample_size: 128
    graph_size: ${graph_size}
    base_nodes_size:
    subsample: False
    k: ${model_cfg.model_args.max_fleet_length}
    cap: 40
    max_cap_factor: # factor of additional capacity w.r.t. a norm capacity of 1.0 per vehicle
    n_depots: 1 #number of depots (default = 1)


decode_v_run_opt: